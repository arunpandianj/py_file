pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    bat 'Python Pipeline'
                    bat 'Python -V'
                }
            }
        }
    }
}
