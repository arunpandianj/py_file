pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    bat 'python --version'
                }
            }
        }
    }
}
