pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    bat 'echo Python'
                    bat 'Python -V'
                }
            }
        }
    }
}
